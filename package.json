{
  "name": "fcc-starter-dev",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "body-parser": "^1.15.2",
    "chalk": "^1.1.3",
    "compression": "^1.6.2",
    "dotenv": "^1.2.0",
    "express": "4.13.3",
    "express-session": "^1.11.3",
    "mongoose": "^4.1.0",
    "passport": "^0.3.0",
    "passport-github": "^1.0.0"
  },
  "devDependencies": {
    "foreman": "1.4.1"
  },
  "scripts": {
    "start":"node server/server.js",
    "build":"cd client && npm install && react-scripts build",
    "dev": "nf start -p 3000 createreactapp,placeholderapi",
    "devapi": "nf start -p 3000 createreactapp,devapi",
    "devserver": "PORT=3001 nodemon server/server.js --ignore client/ --ignore node_modules/",
    "placeholderapi": "API_PORT=3001 nodemon server/server-placeholder.js --ignore client/ --ignore node_modules/"
  }
}
